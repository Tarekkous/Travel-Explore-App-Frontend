import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.travelApp.app',
  appName: 'Travel&Explore',
  webDir: './dist/front-voyage',
  bundledWebRuntime: false
};

export default config;
